"""Рейтинговые агентства и общие тексты раздела."""

from __future__ import annotations

from enum import Enum

_DISPLAY_NAMES: dict[RatingAgency, str]


class RatingAgency(Enum):
    """Рейтинговое агентство.

    Единственный источник правды о наборе агентств. Добавление нового агентства =
    новое значение enum + его скрейпер-провайдер; UI и фильтрация получателей
    подхватывают его автоматически (см. ``AVAILABLE_AGENCIES``).
    """

    NRA = "nra"

    @property
    def display_name(self) -> str:
        """Человекочитаемое имя агентства."""
        return _DISPLAY_NAMES[self]


_DISPLAY_NAMES = {
    RatingAgency.NRA: "НРА",
}

# Агентства, по которым уже есть скрейпер и можно получать уведомления.
# UI рисует тумблер на каждое из них. По мере добавления провайдеров —
# дополняем список.
AVAILABLE_AGENCIES: list[RatingAgency] = [
    RatingAgency.NRA,
]

# Описание раздела для экрана настроек.
RATING_ALERTS_DESCRIPTION = (
    "<b>Кредитные рейтинги</b>\n\n"
    "Получайте уведомление, когда рейтинговое агентство присвоит, повысит, "
    "понизит, подтвердит или отзовёт кредитный рейтинг эмитента, чьи облигации "
    "есть в вашем портфеле.\n\n"
    "Выберите агентства, от которых хотите получать уведомления:"
)
